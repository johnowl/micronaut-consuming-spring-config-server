micronaut:
  application:
    name: micronaut-config-client
  config-client:
    enabled: true
spring:
  cloud:
    config:
      enabled: true
      fail-fast: true
      uri: http://localhost:8888
      label: default